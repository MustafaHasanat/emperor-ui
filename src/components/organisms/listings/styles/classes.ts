import { cva } from "class-variance-authority";

export const listingsClasses = cva([], {
  variants: {
    variant: {
      default: [],
    },
  },
  defaultVariants: {},
  compoundVariants: [],
});

import { cva } from "class-variance-authority";

export const footerClasses = cva([""], {
  variants: {
    variant: {
      default: [],
    },
  },
  defaultVariants: {},
  compoundVariants: [],
});
